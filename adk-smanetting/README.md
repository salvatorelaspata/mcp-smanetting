# Google ADK

## Installation

Using venv
Create a virtual environment:

```bash
python3 -m venv env
```

Activate the virtual environment:

```bash
source .venv/bin/activate
```

To install the Google ADK, run the following command:

```bash
pip install google-adk
```

## Usage
